
# GTSM Imports

Système de suivi et de gestion des importations avec tableaux de bord analytiques et filtrage dynamique.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
